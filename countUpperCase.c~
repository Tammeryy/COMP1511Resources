// Count Upper Case
#include <stdio.h>

#define SIZE 100

int countUpperCase(char str[SIZE]);

int main(void) {
    char str[SIZE];
    // get a string from user input using fgets
    

    return 0;
}

// count how many upper case alphabets are in a string
// return the number 
int countUpperCase(char str[SIZE]) {
    return 0;

}
